class QuipEntity {
  final String id;
  final String userId;
  final String username;
  final String message;
  final String timestamp;

  const QuipEntity(this.id, this.userId, this.username, this.message, this.timestamp);
}
